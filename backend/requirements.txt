# Core web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3

# Data / ML
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2

# Multi-format statement ingestion
openpyxl==3.1.5
xlrd==2.0.2
pdfplumber==0.11.10
chardet==7.4.3

# Environment
python-dotenv==1.0.1

# RAG (Phase 3/4)
chromadb==0.5.23
sentence-transformers==3.3.1
langchain==0.3.9
langchain-core==0.3.21
langgraph==0.2.53

# Multi-provider LLM layer (async HTTP transport for all providers)
httpx==0.28.1
# Optional cache backend — only needed if REDIS_URL is set:
#   pip install redis
# redis==5.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.22.0

# Voice (Phase 6)
gtts==2.5.4
# openai-whisper is heavy (pulls torch). Install manually if needed:
#   pip install openai-whisper
